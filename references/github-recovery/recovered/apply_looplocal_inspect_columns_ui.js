const fs = require('fs'); const { chromium } = require('playwright'); const sql = fs.readFileSync('/Users/productivebot/productivebot/inspect_events_columns.sql','utf8');
(async()=>{ const browser=await chromium.connectOverCDP('http://127.0.0.1:18800'); const context=browser.contexts()[0]||await browser.newContext(); const page=await context.newPage(); let detected=false, response=null; page.on('request', req=>{const pd=req.postData()||''; if(req.method()==='POST' && pd.includes('information_schema.columns') && pd.includes('events')) {detected=true; console.log('INSPECT_COLUMNS_POST_DETECTED');}}); page.on('response', async res=>{const u=res.url(); if(detected && !response && u.includes('/pg-meta/itraeknotcdtdzaeukan/query')){try{response={status:res.status(), text:(await res.text()).slice(0,5000)}}catch{}}}); await page.goto('https://supabase.com/dashboard/project/itraeknotcdtdzaeukan/sql/new',{waitUntil:'domcontentloaded',timeout:60000}); await page.waitForTimeout(8000); await page.evaluate(sql=>window.monaco.editor.getModels()[0].setValue(sql),sql); await page.keyboard.press('Meta+Enter'); await page.waitForTimeout(8000); console.log('INSPECT_COLUMNS_RESPONSE', JSON.stringify(response)); await page.close(); await browser.close(); })();
